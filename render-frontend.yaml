services:
  # Frontend Static Site Only
  - type: web
    name: assignment-frontend
    runtime: static
    buildCommand: cd apps/frontend && pnpm install --frozen-lockfile=false && pnpm run build && cp _redirects dist/
    staticPublishPath: apps/frontend/dist
    envVars:
      - key: NODE_VERSION
        value: 20.20.0
      - key: VITE_API_URL
        value: https://assignment-backend-j6gb.onrender.com